# Auto-generated CMakeLists.txt
# Remove this header to avoid further auto-generation
# File derived from Makefile in 'h5stat\Makefile'

#Sources 0
file(GLOB h5stat_src "*.c")

# Target
add_vishexe(h5stat  ${h5stat_src})
