# Auto-generated CMakeLists.txt
# Remove this header to avoid further auto-generation
# File derived from Makefile in 'h5ls\Makefile'

#Sources 0
file(GLOB h5ls_src "*.c")

# Target
add_vishexe(h5ls  ${h5ls_src})
