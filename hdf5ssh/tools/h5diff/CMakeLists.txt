# Auto-generated CMakeLists.txt
# Remove this header to avoid further auto-generation
# File derived from Makefile in 'h5diff\Makefile'

#Sources 0
file(GLOB h5diff_src "*.c")

# Target
add_vishexe(h5diff  ${h5diff_src})
