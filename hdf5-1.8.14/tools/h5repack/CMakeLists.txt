# Auto-generated CMakeLists.txt
# Remove this header to avoid further auto-generation
# File derived from Makefile in 'h5repack\Makefile'

#Sources 0
file(GLOB h5repack_src "*.c" "*.h")

# Target
add_vishexe(h5repack  ${h5repack_src})
