# Auto-generated CMakeLists.txt
# Remove this header to avoid further auto-generation
# File derived from Makefile in 'h5dump\Makefile'

#Sources 0
file(GLOB h5dump_src "*.c" "*.h")

# Target
add_vishexe(h5dump  ${h5dump_src})
