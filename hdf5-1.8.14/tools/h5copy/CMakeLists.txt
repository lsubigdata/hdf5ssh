# Auto-generated CMakeLists.txt
# Remove this header to avoid further auto-generation
# File derived from Makefile in 'h5copy\Makefile'

#Sources 0
file(GLOB h5copy_src "*.c" "*.h")

# Target
add_vishexe(h5copy  ${h5copy_src})
