# Auto-generated CMakeLists.txt
# Remove this header to avoid further auto-generation
# File derived from Makefile in 'h5jam\Makefile'

#Sources 0
file(GLOB h5jam_src "*.c" "*.h")

# Target
add_vishexe(h5jam  ${h5jam_src})
